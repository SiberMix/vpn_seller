services:
  marzban:
    build:
      context: .
      dockerfile: Marzban-master/Dockerfile
    restart: always
    env_file: .env
    network_mode: host
    volumes:
      - /var/lib/marzban:/var/lib/marzban
